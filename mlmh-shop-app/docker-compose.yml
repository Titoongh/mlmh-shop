services:
    mlmh_shop:
        build:
            context: .
            dockerfile: Dockerfile
            target: development
        ports:
            - '3000:3000'
        environment:
            NODE_ENV: development
            NEXT_PUBLIC_API_URL: http://localhost:3000
